name = "al-ribat-manager"
pages_build_output_dir = "dist"
compatibility_date = "2024-03-08"

[build]
command = "npm run build"

[vars]
# Note: Do not put your VITE_SUPABASE_URL or VITE_SUPABASE_PUBLISHABLE_KEY here if you are committing to a public repo.
# Instead, add them in the Cloudflare Pages dashboard under Settings -> Environment variables.
